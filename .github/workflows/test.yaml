name: Day 1 basics

on: 
    push: {}
    workflow_dispatch: {}

jobs:
    example:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: echo "hello From Github Actions"